# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Harness runtime state (workspace is data, not source)
workspace/

# Local config with secrets
config.yaml
.env

# Generated by deploy/inference/start.sh (the launch command it assembles)
deploy/inference/.generated/

# Raw benchmark measurements accumulate per machine; the FROZEN profile
# (config/inference-tuning.json) is meant to be committed, this is not.
config/benchmark-results.json
