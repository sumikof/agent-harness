"""Long-running coding agent harness.

Sequential multi-role agents (Planner / Analyst / Developer / Tester /
Reviewer / Diagnostician) orchestrated by a deterministic state machine,
with SQLite + Git + structured artifacts as the persistent state.
"""

__version__ = "0.1.0"
